import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Title like page',
    description: 'description like page',
}

const LikePage = () => {
    return (
        <div>LikePage</div>
    )
}

export default LikePage;